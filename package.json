{
  "name": "meattrack-mobile",
  "private": true,
  "version": "1.0.0",
  "description": "Capacitor mobile shell for the MEATTRACK FastAPI application",
  "scripts": {
    "mobile:sync": "cap sync",
    "mobile:android": "cap open android",
    "mobile:run:android": "cap run android",
    "mobile:doctor": "cap doctor"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/core": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "typescript": "^5.9.0"
  }
}
